/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns: [
            {
                hostname:"phztnztrdvdbziposzlf.supabase.co"
            }
        ]
    }
}

module.exports = nextConfig
